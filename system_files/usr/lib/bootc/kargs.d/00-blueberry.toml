# Framework AMD AI 300-series workarounds. Without these, the screen
# flickers under load and the GPU intermittently freezes.
kargs = [
    "amdgpu.sg_display=0",
    "amdgpu.dcdebugmask=0x10",
    "amdgpu.abm_level=0",
]
match-architectures = ["x86_64"]
