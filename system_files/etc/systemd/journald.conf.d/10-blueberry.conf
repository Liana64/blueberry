[Journal]
# Persist across reboots so `journalctl --boot=-1` and `ujust anomalies` work.
Storage=persistent

# SystemKeepFree wins under disk pressure.
SystemMaxUse=500M
SystemKeepFree=1G

# Smaller files = faster grep / easier rotation.
SystemMaxFileSize=50M
